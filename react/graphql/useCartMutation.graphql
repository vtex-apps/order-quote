mutation useCartMutation(
  $orderFormId: String
  $items: [CartUseInfo]
  $customData: CustomDataInput
  $userType: String
) {
  useCart(
    orderFormId: $orderFormId
    items: $items
    customData: $customData
    userType: $userType
  )
}
